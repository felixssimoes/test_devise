<h1>Users</h1>

<p><%= link_to "Invite User", new_user_invitation_path, class: "btn btn-primary" %>

<table class="table">
  <% @users.each do |user| %>
  <tr>
    <td>
      <%= user.name %>
    </td>
    <td>
      <%= user.email %>
    </td>
    <td>
      <%= link_to "Edit", edit_user_path(user), class: "btn btn-info btn-xs" %>
      <%- if can? :edit, user %>
        <%= link_to "Edit", edit_user_path(user), class: "btn btn-info btn-xs" %>
      <% end -%>
    </td>
  </tr>
  <% end  %>
</table>
